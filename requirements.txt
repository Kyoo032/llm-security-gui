# LLM Security GUI - Requirements
# GTK/PyGObject is expected from system packages.
# Recommended venv mode:
#   python3 -m venv .venv --system-site-packages
# Plain isolated venvs may try compiling PyGObject and fail without native build deps.
PyGObject
huggingface_hub[cli]>=0.20.0
requests>=2.31.0
